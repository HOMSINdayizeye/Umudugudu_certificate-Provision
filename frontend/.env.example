# Base URL of the Django backend, without a trailing slash.
# Leave empty locally: the Vite dev server proxies /api to http://127.0.0.1:8000.
# On Vercel set it to the backend project's URL, e.g. https://certify-backend.vercel.app
VITE_API_URL=https://umudugudu-certificate-provision.onrender.com
